# Expected violations for suppress-next.lisp
# Format: line:column rule-name severity
# Only the second function (bar) should have a violation
# The first function (foo) has suppress-next, so its violation is suppressed

16:0 if-without-else convention
